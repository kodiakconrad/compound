import { Text, View } from "react-native";

// Library tab — placeholder until Step 3 implements exercise list, filters, and create.
export default function LibraryScreen() {
  return (
    <View className="flex-1 bg-background items-center justify-center">
      <Text className="text-white text-xl font-bold">Library</Text>
      <Text className="text-muted mt-2">Coming in Step 3</Text>
    </View>
  );
}
